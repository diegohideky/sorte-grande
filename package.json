{
  "name": "sorte-grande",
  "version": "1.0.0",
  "description": "",
  "main": "app/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager-interactive",
    "package:mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=app/icon.icns --prune=true --out=release",
    "package:win": "electron-packager . --overwrite --icon=app/icon.ico --platform=win32 --arch=x64 --prune=true --out=release",
    "package:linux": "electron-packager . appointment --overwrite --asar=true --platform=linux --arch=x64 --icon=app/icon.png --prune=true --out=release"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^1.8.4",
    "electron-packager": "^12.0.1",
    "electron-packager-interactive": "^0.1.3"
  },
  "dependencies": {
    "jquery": "^3.3.1",
    "materialize-css": "^0.100.2"
  }
}
